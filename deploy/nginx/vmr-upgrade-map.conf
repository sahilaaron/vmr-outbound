# Installs to /etc/nginx/conf.d/vmr-upgrade-map.conf
# Required by snippets/vmr-proxy.conf. Safe when no WebSocket traffic exists.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
