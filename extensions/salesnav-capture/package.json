{
  "name": "vmr-salesnav-capture",
  "version": "2.2.0",
  "private": true,
  "description": "Operator-driven contact-acquisition Chrome extension for the VMR Outbound Agent (dev/test tooling only; the extension itself ships zero runtime dependencies).",
  "type": "commonjs",
  "scripts": {
    "test": "node --test",
    "mock-receiver": "node tools/mock-receiver.js"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
