{
  "name": "vmr-google-sheets-addon",
  "version": "1.0.0",
  "private": true,
  "description": "Google Sheets add-on for the VMR Outbound Agent (dev/test tooling only; the add-on itself ships zero runtime dependencies).",
  "type": "commonjs",
  "scripts": {
    "test": "node --test"
  }
}
